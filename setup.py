from setuptools import setup

install_requires = [
    # list the required library
]

packages = [
    'model_train',
]



setup(
    name='model_train',
    version='0.1',
    packages=packages,
    install_requires=install_requires,
)